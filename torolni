const express = require('express');
const mysql = require('mysql');
const bodyParser = require('body-parser');
const path = require('path');

const app = express();
const port = 8889; // Choose your desired port

// Database connection setup
const db = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    password: 'root',
    database: 'com710database'
});

// Connect to the database
db.connect((err) => {
    if (err) {
        throw err;
    }
    console.log('MySQL database connected');
});

// Middleware setup
app.use(bodyParser.json());
app.use(bodyParser.urlencoded({ extended: true }));

// Route to fetch data from the database and render the HTML page
app.get('/', (req, res) => {
    // Query to retrieve data from the database
    const sql = 'SELECT * FROM modules';

    // Execute the query
    db.query(sql, (err, rows) => {
        if (err) {
            res.status(500).send('Database query error');
            throw err;
        }

        // Render the HTML page with the retrieved data
        res.sendFile(path.join(__dirname, 'index.html'), { modulesData: rows });
    });
});

// Start the server
app.listen(port, () => {
    console.log(`Server is running on port ${port}`);
});






<h1>Modules Introduction</h1>

<table id="modules-table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Semester</th>
        </tr>
    </thead>
    <tbody>
        <!-- Data rows will be populated here dynamically -->
        <% modulesData.forEach(module => { %>
            <tr>
                <td><%= module.id %></td>
                <td><%= module.name %></td>
                <td><%= module.semester %></td>
            </tr>
        <% }); %>
    </tbody>
</table>


<table id="modules-table">
<thead>
    <tr>
        <th>Id Code</th>
        <th>Module Name</th>
        <th>Semester</th>
        <th>Description</th>
    </tr>
</thead>
<tbody>
    <% modulesData.forEach(module => { %>
    <tr>
        <td><%= module.id %></td>
        <td><%= module.name %></td>
        <td><%= module.semester %></td>
        <td><%= module.description %></td>
    </tr>
    <% }); %>
</tbody>
</table>






const express = require('express');
const mysql = require('mysql');
const bodyParser = require('body-parser');
const path = require('path');

const app = express();
const port = 8889; // Choose your desired port

// Database connection setup
const db = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    password: 'root',
    database: 'com710database'
});

// Connect to the database
db.connect((err) => {
    if (err) {
        throw err;
    }
    console.log('MySQL database connected');
});

// Middleware setup
app.use(bodyParser.json());
app.use(bodyParser.urlencoded({ extended: true }));

// Set the view engine to EJS
app.set('view engine', 'ejs');
app.set('views', path.join(__dirname, 'views'));

// Route to fetch data from the database and render the HTML page using EJS
app.get('/', (req, res) => {
    // Query to retrieve data from the database
    const sql = 'SELECT * FROM modules';

    // Execute the query
    db.query(sql, (err, rows) => {
        if (err) {
            res.status(500).send('Database query error');
            throw err;
        }

        // Render the HTML page (modules.ejs) with the retrieved data
        res.render('modules', { modulesData: rows });
    });
});

// Serve static files (CSS, images, etc.)
app.use(express.static(path.join(__dirname, 'public')));

// Start the server
app.listen(port, () => {
    console.log(`Server is running on port ${port}`);
});





------
const express = require('express');
const mysql = require('mysql');
const bodyParser = require('body-parser');
const path = require('path');

const app = express();
const port = 8889;

// Database connection setup
const db = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    password: 'root',
    database: 'com710database'
});

// Connect to the database
db.connect((err) => {
    if (err) {
        throw err;
    }
    console.log('MySQL database connected');
});

// Middleware setup
app.use(bodyParser.json());
app.use(bodyParser.urlencoded({ extended: true }));

// Set the view engine to EJS
app.set('view engine', 'ejs');
app.set('views', path.join(__dirname, 'views'));

// Route to fetch data from the database and render the HTML page using EJS
app.get('/', (req, res) => {
    const sql = 'SELECT * FROM modules';
    db.query(sql, (err, rows) => {
        if (err) {
            res.status(500).send('Database query error');
            throw err;
        }
        res.render('modules', { modulesData: rows });
    });
});

// Serve static files from the 'public' directory
app.use(express.static(path.join(__dirname, 'public')));

// Start the server
app.listen(port, () => {
    console.log(`Server is running on port ${port}`);
});





const express = require('express');
const app = express();
const path = require('path');

app.set('view engine', 'ejs');
app.set('views', path.join(__dirname, 'views'));




app.get('/', (req, res) => {
    const sql = 'SELECT * FROM modules';
    db.query(sql, (err, rows) => {
        if (err) {
            res.status(500).send('Database query error');
            throw err;
        }
        res.render('modules', { modules: rows });
    });
});




// Express útvonal a modul oldalhoz
app.get('/modules', (req, res) => {
    // Adatok lekérése az adatbázisból
    const sql = 'SELECT * FROM modules';
    db.query(sql, (err, rows) => {
        if (err) {
            res.status(500).send('Database query error');
            throw err;
        }
        // Az EJS fájl renderelése és átadása az adatokkal
        res.render('modules', { modulesData: rows });
    });
});




const express = require('express');
const app = express();

app.set('view engine', 'ejs'); // Nézetmotor beállítása az EJS-re
app.set('views', path.join(__dirname, 'views')); // Nézetek mappájának beállítása

// Egyéb konfigurációk és útvonalak...



app.get('/', (req, res) => {
    const sql = 'SELECT * FROM modules';
    db.query(sql, (err, rows) => {
        if (err) {
            res.status(500).send('Database query error');
            throw err;
        }
        // Az EJS nézet renderelése és küldése a kliensnek
        res.render('modules', { modulesData: rows });
    });
});




<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Modules</title>
    <link rel="stylesheet" href="css/style.css">
    <script src="https://kit.fontawesome.com/b17e95f49f.js" crossorigin="anonymous"></script>
    <!-- Font Awesome script -->
</head>

<body>
    <div class="container">
        <div class="header">
            <div class="solentLogo">
                <img src="https://www.solent.ac.uk/graphics/logo/rebrandLogo.svg" alt="Logo">
            </div>
            <p class="headerParagraph">SOLENT University Southampton</p>
        </div>

        <div class="navigation">
            <ul class="navbar">
                <li><a class="nav" href="index.html">About</a></li>
                <li><a class="nav" href="activities.html">Activities</a></li>
                <li><a class="nav" href="modules.html">Modules</a></li>
                <li><a class="nav" href="registration.html">Registration</a></li>
            </ul>
        </div>

        <h1>Modules Introduction</h1>

        <table id="modules-table">
            <thead>
                <tr>
                    <th>Id Code</th>
                    <th>Module Name</th>
                    <th>Semester</th>
                    <th>Description</th>
                </tr>
            </thead>
            <tbody>
                <% if (modulesData.length !== 0) { %>
                    <% modulesData.forEach(module => { %>
                        <tr>
                            <td><%= module.idCode %></td>
                            <td><%= module.moduleName %></td>
                            <td><%= module.semester %></td>
                            <td><%= module.description %></td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="4">No modules found</td>
                    </tr>
                <% } %>
            </tbody>
        </table>

        <!-- Footer -->
        <div class="footer-container">
            <div class="sub-footer">
                <h1>Our Address</h1>
                <p>Solent University, East Park Terrace, Southampton, SO14 0YN</p>
                <div class="visitorCounter">
                    <h1>Visitor counter</h1>
                    <p id="visitorCounter">Here should be the number of visitors!</p>
                </div>
            </div>
            <div class="sub-footer">
                <h1>About</h1>
                <p id="languagePar">Choose a language and you will know my opinion!</p>
                <p>Select Language</p>
                <select data-placeholder="Choose a Language...">
                    <option>Choose a language!</option>
                    <option value="EN">English</option>
                    <option value="HU">Hungarian</option>
                    <option value="GE">German</option>
                    <option value="FR">French</option>
                </select>
            </div>
            <div class="sub-footer">
                <h1>Follow us!</h1>
                <p>Click on the icons to find us on social media!</p>
                <ul class="social-icons">
                    <li>Facebook <a href="https://www.facebook.com/SolentUniversity/?locale=en_GB" class="icon-link"><i class="fab fa-facebook-f"></i></a></li>
                    <li>Twitter <a href="https://twitter.com/solentuni" class="icon-link"><i class="fab fa-twitter"></i></a></li>
                    <li>Instagram <a href="https://www.instagram.com/solentuniversity/" class="icon-link"><i class="fab fa-instagram"></i></a></li>
                    <li>LinkedIn <a href="https://www.linkedin.com/school/solentuni/" class="icon-link"><i class="fab fa-linkedin"></i></a></li>
                    <li>Apple <a href="https://www.apple.co.uk/" class="icon-link"><i class="fab fa-apple"></i></a></li>
                    <li>Github <a href="#" class="icon-link"><i class="fab fa-github"></i></a></li>
                </ul>
            </div>
        </div>
    </div>

    <script src="myScript.js"></script>
</body>

</html>








// server.js

const express = require('express');
const mysql = require('mysql');
const bodyParser = require('body-parser');
const path = require('path');

const app = express();
const port = 8889;

// Database connection setup
const db = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    password: 'root',
    database: 'com710database'
});

// Connect to the database
db.connect((err) => {
    if (err) {
        throw err;
    }
    console.log('MySQL database connected');
});

// Middleware setup
app.use(bodyParser.json());
app.use(bodyParser.urlencoded({ extended: true }));

// Set the view engine to EJS and specify the views directory
app.set('view engine', 'ejs');
app.set('views', path.join(__dirname, 'views'));

// Handle registration form submission
app.post('/register', (req, res) => {
    const { fname, lname, gender, email, mobile, password, comment } = req.body;

    const sql = `INSERT INTO register (firstName, lastName, gender, email, mobile, password, comment) VALUES (?, ?, ?, ?, ?, ?, ?)`;
    db.query(sql, [fname, lname, gender, email, mobile, password, comment], (err, result) => {
        if (err) {
            res.status(500).send('Hiba történt a regisztráció során');
            throw err;
        }
        res.status(200).send('Sikeres regisztráció');
    });

    console.log('Kapott adatok:', req.body);
});

// Route to fetch data from the database and render the modules.ejs view
app.get('/', (req, res) => {
    const sql = 'SELECT * FROM modules';
    db.query(sql, (err, rows) => {
        if (err) {
            res.status(500).send('Adatbázis lekérdezési hiba');
            throw err;
        }
        res.render('modules', { modulesData: rows });
    });
});

// Serve static files from the 'public' directory
app.use(express.static(path.join(__dirname, 'public')));

// Start the server
app.listen(port, () => {
    console.log(`A szerver fut a http://localhost:${port} címen`);
});




----------------




<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Modules</title>
  <link rel="stylesheet" href="css/style.css">
  <script src="https://kit.fontawesome.com/b17e95f49f.js" crossorigin="anonymous"></script>
</head>

<body>
    <div class="container">

        <div class="header">
            <div class="solentLogo">
                <img src="https://www.solent.ac.uk/graphics/logo/rebrandLogo.svg" alt="Logo">
            </div>
            <p class="headerParagraph">SOLENT University Southampton</p>
        </div>

        <div class="navigation">
            <ul class="navbar">
                <li><a class="nav" href="index.html">About</a></li>
                <li><a class="nav" href="activities.html">Activities</a></li>
                <li><a class="nav" href="modules.html">Modules</a></li>
                <li><a class="nav" href="registration.html">Registration</a></li>
            </ul>
        </div>

        <h1>Modules Introduction</h1>

        <table id="modules-table">
            <thead>
                <tr>
                    <th>Id Code <button onclick="sortTable('idCode')">Sort</button></th>
                    <th>Module Name <button onclick="sortTable('moduleName')">Sort</button></th>
                    <th>Semester <button onclick="sortTable('semester')">Sort</button></th>
                    <th>Description <button onclick="sortTable('description')">Sort</button></th>
                </tr>
            </thead>
            <tbody>
                <% if (data.length !== 0) { %>
                    <% data.forEach(function(item) { %>
                        <tr>
                            <td><%= item.idCode %></td>
                            <td><%= item.moduleName %></td>
                            <td><%= item.semester %></td>
                            <td><%= item.description %></td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="4">No data available</td>
                    </tr>
                <% } %>
            </tbody>
        </table>

        <!-- Footer Section -->
        <div class="footer-container">
            <div class="sub-footer">
                <h1>Our Address</h1>
                <p>Solent University, East Park Terrace, Southampton, SO14 0YN</p>
                <div class="visitorCounter">
                    <h1>Visitor counter</h1>
                    <p id="visitorCounter">Here should be the number of visitors!</p>
                </div>
            </div>
            <div class="sub-footer">
                <h1>About</h1>
                <p id="languagePar">Choose a language and you will know my opinion!</p>
                <p>Select Language</p>
                <select data-placeholder="Choose a Language...">
                    <option>Choose a language!</option>
                    <option value="EN">English</option>
                    <option value="HU">Hungarian</option>
                    <option value="GE">German</option>
                    <option value="FR">French</option>
                </select>
            </div>
            <div class="sub-footer">
                <h1>Follow us!</h1>
                <p>Click on the icons to find us on social media!</p>
                <ul class="social-icons">
                    <li>Facebook <a href="https://www.facebook.com/SolentUniversity/?locale=en_GB" class="icon-link"><i class="fab fa-facebook-f"></i></a></li>
                    <li>Twitter <a href="https://twitter.com/solentuni" class="icon-link"><i class="fab fa-twitter"></i></a></li>
                    <li>Instagram <a href="https://www.instagram.com/solentuniversity/" class="icon-link"><i class="fab fa-instagram"></i></a></li>
                    <li>LinkedIn <a href="https://www.linkedin.com/school/solentuni/" class="icon-link"><i class="fab fa-linkedin"></i></a></li>
                    <li>Apple <a href="https://www.apple.co.uk/" class="icon-link"><i class="fab fa-apple"></i></a></li>
                    <li>Github <a href="#" class="icon-link"><i class="fab fa-github"></i></a></li>
                </ul>
            </div>
        </div>

    </div>

    <script src="myScript.js"></script>
</body>

</html>






<tbody>
                <% if (modulesData.length !== 0) { %>
                    <% modulesData.forEach(function(module) { %>
                        <tr>
                            <td><%= module.idCode %></td>
                            <td><%= module.moduleName %></td>
                            <td><%= module.semester %></td>
                            <td><%= module.description %></td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="4">No modules found</td>
                    </tr>
                <% } %>
            </tbody>